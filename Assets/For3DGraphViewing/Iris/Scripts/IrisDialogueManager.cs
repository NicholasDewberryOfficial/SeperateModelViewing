using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class IrisDialogueManager : MonoBehaviour
{
    //public IrisFluentScript IrisFluentScript;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


      void OnMouseDown(){ 
        //IrisfluentScript.Run();
    }
    // if ui isn't being used, then destroy it
//     public void Awake()
//         {
//             if (instance == null)
//             {
//                 DontDestroyOnLoad(gameObject);
//                 instance = this;
//             } else if (instance != null)
//             {
//                 Destroy(gameObject);
//             }
//         }

}
